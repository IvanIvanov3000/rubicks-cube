<div class="cube">
    <img class="cube" src={{image}}>
    <p class="name">{{name}}</p>
    <p><span>Difficulty level:</span> {{difficulty}}</p>
    <a class="btn" href="/cube/{{id}}">Details</a>
</div>